# Tools

Only `active/` competes for development attention.

- `active/` — under development; each project needs a concrete next test.
- `upcoming/` — ideas and plans I am not building now.
- `stable/` — working tools and settled references; revisit them when they break.

Classify by the attention a project should receive, not its latest commit. Moving a
folder is the status change.
