# Active projects

These are the only projects competing for development attention.

- `my-vm/` — built today; the first real provision-and-delegate run still has to prove it.
- `credentials/` — add secret scanning and keep narrowing what agents can reach.
- `dictation/` — the working FluidVoice setup and Murmur replacement now live together.
- `gdoc/` — the hosted connector still needs its production OAuth and organization deployment.
- `gws/` — keep improving the Google Workspace CLI and bounded Drive access.
- `search-bar/` — the app works, but connected-source search remains unfinished.
- `shortcuts/` — turn the conservative base into the shortcut system I actually want.
- `twitter/` — maintain the extension and finish the graph and list workflows.
