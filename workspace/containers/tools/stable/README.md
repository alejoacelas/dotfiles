# Stable projects

These work or serve as settled references. Revisit them when they break.

- `cmd-tab/` — the Command-Tab app I am happy with and currently run.
- `fetch-url/` — the implemented URL retrieval CLI.
- `sleep/` — the installed agent-aware sleep daemon.
- `spotlight/` — the installed Launcher app.

- `model-router-check/` — daily launchd smoke test that GPT routing and the 1M Claude window still work.

- `roughdraft/` — the installed Markdown review app.
