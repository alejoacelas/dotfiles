interface:
  display_name: "Human Edit Tracking"
  short_description: "Preserve marked human edits in Markdown"
  default_prompt: "Use $human-edit-tracking to inspect and record human edits in a Markdown file whose changes carry the ;; marker."
